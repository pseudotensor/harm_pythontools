import visit_writer
import numpy as np

def genmesh():
    x = r*np.sin(h)*cos(ph)
    y = r*np.sin(h)*sin(ph)
    z = r*np.cos(h)
    pts = np.array(zip(x,y,z)).reshape(-1)
    


if __name__ == "__main__":
    return
